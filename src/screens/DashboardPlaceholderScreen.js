import React from 'react';
import LocationRouteScreen from './LocationRouteScreen';

/**
 * DashboardPlaceholderScreen (Now routed to Master LocationRouteScreen)
 */
export const DashboardPlaceholderScreen = (props) => {
  return <LocationRouteScreen {...props} />;
};

export default DashboardPlaceholderScreen;
